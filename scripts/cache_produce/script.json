{
  "id": "cache_produce",
  "name": "Cache Produce",
  "description": "Creates a numpy array and caches it in shared memory. Run Cache Consume while this is still running.",
  "params": [
    {
      "name": "key",
      "required": false,
      "default": "shared_array",
      "description": "Cache key name"
    },
    {
      "name": "size",
      "required": false,
      "default": "100",
      "description": "Array size"
    },
    {
      "name": "hold_seconds",
      "required": false,
      "default": "30",
      "description": "How long to keep the shared memory handle alive"
    }
  ]
}
