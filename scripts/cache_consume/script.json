{
  "id": "cache_consume",
  "name": "Cache Consume",
  "description": "Retrieves a cached numpy array from shared memory. Run while Cache Produce is still holding.",
  "params": [
    {
      "name": "key",
      "required": false,
      "default": "shared_array",
      "description": "Cache key to retrieve"
    }
  ]
}
